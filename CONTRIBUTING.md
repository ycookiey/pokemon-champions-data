# 貢献ガイド

未収集ポケモンの技データ収集を歓迎します。動画を録画して処理し、結果を PR で追加するだけです。

## 手順

1. **ステータスページで未収集を確認**: https://ycookiey.github.io/pokemon-champions-data/
   （重複録画を避けるため、録画前に必ず確認してください）
2. **録画・処理**: 収集ツール ch-data-collector の録画手順に従って動画を録画し、技データを抽出
3. **データ追加**: `data/collected.json` にポケモン名をキーとして技リストを追加
4. **PR を作成**: マージされると CI が収集状況を自動更新します

## データ形式

`data/collected.json` は `{ポケモン名: [技名...]}` のオブジェクト。追加例:

```json
{
  "ピカチュウ": ["10まんボルト", "でんこうせっか", "..."]
}
```

- キー順は五十音順に保つと PR の衝突を避けやすい

## ポケモン名の表記ルール

キーは、**母集合 `data/pokemon.json` の名前と完全一致**させてください（一致しないと収集状況に反映されません）。PR 時に CI（`scripts/validate_collected.py`）が母集合との一致・形式・技の重複を自動検証し、不一致があれば失敗します。

- フォーム違いは母集合の表記に合わせる:
  - `ライチュウ(アローラ)`, `ロトム(ヒート)` 等
- 技プールが元のフォームと共通のポケモン（メガシンカ等）は収集対象外とする

## ライセンス

PR を送ることで、追加するデータを **CC0 1.0**（パブリックドメイン献納、`data/LICENSE`）で提供することに同意したものとみなします。技名・ポケモン名等の原著作権は (株) ポケモン / 任天堂 / ゲームフリーク / クリーチャーズ に帰属します。
